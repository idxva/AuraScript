# Copy this file to .env for local backend development. Never commit .env.
#
# Add server-only secrets here only when server.py needs them. For example:
# GOOGLE_API_KEY=replace_with_a_new_restricted_key
#
# On Render, create the same variables in the service's Environment settings.
# Do not place secrets in JavaScript, HTML, or frontend build configuration.